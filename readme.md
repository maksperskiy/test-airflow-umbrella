1. Add module name and path to terget folder in "modules.cfg" file
```
{MODULENAME} {BRANCH} {NAME} {TARGET_PATH}
# empty line
```

To remove submodule remove if from the "modules.cfg" and run
```sh
rm -rf {MODULENAME}
```